#include <stdio.h>
#include <math.h>
#include <stdbool.h>

bool isPrime(int num)
{
  int i,j;
  for(i=0;i<sqrt(num);i++)
    {
      if(num%i==0)
	return false;
    }
  return true;
}


int main()
{
  
}
