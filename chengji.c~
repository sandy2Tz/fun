#include <stdio.h>

int main()
{
  int grade;
  scanf("%i",&grade);
  
  if(grade>=90)
    printf("A\n");
  else if(grade>=80 && grade<=89)
    printf("B\n");
  else if(grade>=70 && grade<=79)
    printf("C\n");
  else 
    printf("D\n");
  return 0;
}
